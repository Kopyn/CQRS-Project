name: CI/CD workflow for command-handling transaction-service

on:
  pull_request:
    branches:
      - main
    paths:
      - 'transaction-service/**'

jobs:
  build:
    runs-on: ubuntu:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
  dockerize:
    runs-on: ubuntu:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      
