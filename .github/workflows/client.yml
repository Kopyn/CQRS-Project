name: Client app CI/CD

on:
  push:
    branches: [ "main" ]
    paths: 
      - 'client/**'
  pull_request:
    branches: [ "main" ]
    paths: 
      - 'client/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B --file pom.xml clean package
  docker:
      runs-on: ubuntu-latest
      needs: build
      steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Log in to Github Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Docker image
        run: docker build -t ghcr.io/${{ toLower(github.repository) }}/client:latest ./client
      - name: Push Docker image
        run: docker push ghcr.io/${{ toLower(github.repository) }}/client:latest
